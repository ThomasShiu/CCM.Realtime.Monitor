@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Index.cshtml";
}
<script>
    $(function () {
        gridList();
    })
    function gridList() {
        var $gridList = $("#gridList");
        $gridList.dataGrid({
            url: "/ERPManage/SS03/GetGridJson",
            height: $(window).height() - 128,
            colModel: [
					{ label: "SID", name: "SID", hidden: true, key: true, width: 80, align: 'left' },
				                     { label: "帳號", name: "USR_NO", width: 80, align: 'left' },
                                     { label: "姓名", name: "USR_NM", width: 90, align: 'left' },
				                     { label: "程式代碼", name: "PRG_NO", width: 80, align: 'left' },
				                     { label: "程式名稱", name: "PRG_NM", width: 150, align: 'left' },
				                     {
				                         label: "執行", name: "C_RUN", width: 50, align: 'left',
				                         formatter: function (cellvalue, options, rowObject) {
				                             if (cellvalue == 'Y') {
				                                 return '允許';
				                             } else if (cellvalue == 'N') {
				                                 return '<span style=\"color:red\">禁用</span>';
				                             } else if (cellvalue == '1') {
				                                 return '個人';
				                             } else{
				                                 return '群祖';
				                             }
				                         }
				                     },
				                     {
				                         label: "新增", name: "C_ADD", width: 50, align: 'left',
				                         formatter: function (cellvalue, options, rowObject) {
				                             if (cellvalue == 'Y') {
				                                 return '允許';
				                             } else if (cellvalue == 'N') {
				                                 return '<span style=\"color:red\">禁用</span>';
				                             } else if (cellvalue == '1') {
				                                 return '個人';
				                             } else {
				                                 return '群祖';
				                             }
				                         }
				                     },
				                     {
				                         label: "查詢", name: "C_QRY", width: 50, align: 'left',
				                         formatter: function (cellvalue, options, rowObject) {
				                             if (cellvalue == 'Y') {
				                                 return '允許';
				                             } else if (cellvalue == 'N') {
				                                 return '<span style=\"color:red\">禁用</span>';
				                             } else if (cellvalue == '1') {
				                                 return '個人';
				                             } else {
				                                 return '群祖';
				                             }
				                         }
				                     },
				                     {
				                         label: "修改", name: "C_MDY", width: 50, align: 'left',
				                         formatter: function (cellvalue, options, rowObject) {
				                             if (cellvalue == 'Y') {
				                                 return '允許';
				                             } else if (cellvalue == 'N') {
				                                 return '<span style=\"color:red\">禁用</span>';
				                             } else if (cellvalue == '1') {
				                                 return '個人';
				                             } else {
				                                 return '群祖';
				                             }
				                         }
				                     },
				                     {
				                         label: "確認", name: "C_CFM", width: 50, align: 'left',
				                         formatter: function (cellvalue, options, rowObject) {
				                             if (cellvalue == 'Y') {
				                                 return '允許';
				                             } else if (cellvalue == 'N') {
				                                 return '<span style=\"color:red\">禁用</span>';
				                             } else if (cellvalue == '1') {
				                                 return '個人';
				                             } else {
				                                 return '群祖';
				                             }
				                         }
				                     },
				                     {
				                         label: "還原", name: "C_UCF", width: 50, align: 'left',
				                         formatter: function (cellvalue, options, rowObject) {
				                             if (cellvalue == 'Y') {
				                                 return '允許';
				                             } else if (cellvalue == 'N') {
				                                 return '<span style=\"color:red\">禁用</span>';
				                             } else if (cellvalue == '1') {
				                                 return '個人';
				                             } else {
				                                 return '群祖';
				                             }
				                         }
				                     },
				                     {
				                         label: "刪除", name: "C_DEL", width: 50, align: 'left',
				                         formatter: function (cellvalue, options, rowObject) {
				                         if (cellvalue == 'Y') {
				                             return '允許';
				                         } else if (cellvalue == 'N') {
				                             return '<span style=\"color:red\">禁用</span>';
				                         } else if (cellvalue == '1') {
				                             return '個人';
				                         } else{
				                             return '群祖';
				                         }
				                     } },
				                     {
				                         label: "金額", name: "C_CST", width: 50, align: 'left',
				                         formatter: function (cellvalue, options, rowObject) {
				                             if (cellvalue == 'Y') {
				                                 return '允許';
				                             } else if (cellvalue == 'N') {
				                                 return '<span style=\"color:red\">禁用</span>';
				                             } else if (cellvalue == '1') {
				                                 return '個人';
				                             } else {
				                                 return '群祖';
				                             }
				                         }
				                     },
				                     {
				                         label: "複製", name: "C_CPY", width: 50, align: 'left',
				                         formatter: function (cellvalue, options, rowObject) {
				                             if (cellvalue == 'Y') {
				                                 return '允許';
				                             } else if (cellvalue == 'N') {
				                                 return '<span style=\"color:red\">禁用</span>';
				                             } else if (cellvalue == '1') {
				                                 return '個人';
				                             } else {
				                                 return '群祖';
				                             }
				                         }
				                     },
				                     {
				                         label: "郵件", name: "C_EML", width: 50, align: 'left',
				                         formatter: function (cellvalue, options, rowObject) {
				                             if (cellvalue == 'Y') {
				                                 return '允許';
				                             } else if (cellvalue == 'N') {
				                                 return '<span style=\"color:red\">禁用</span>';
				                             } else if (cellvalue == '1') {
				                                 return '個人';
				                             } else {
				                                 return '群祖';
				                             }
				                         }
				                     },
				                     {
				                         label: "附檔", name: "C_ATT", width: 50, align: 'left',
				                         formatter: function (cellvalue, options, rowObject) {
				                             if (cellvalue == 'Y') {
				                                 return '允許';
				                             } else if (cellvalue == 'N') {
				                                 return '<span style=\"color:red\">禁用</span>';
				                             } else if (cellvalue == '1') {
				                                 return '個人';
				                             } else {
				                                 return '群祖';
				                             }
				                         }
				                     },
				                     {
				                         label: "送簽", name: "C_SIN", width: 50, align: 'left',
				                         formatter: function (cellvalue, options, rowObject) {
				                             if (cellvalue == 'Y') {
				                                 return '允許';
				                             } else if (cellvalue == 'N') {
				                                 return '<span style=\"color:red\">禁用</span>';
				                             } else if (cellvalue == '1') {
				                                 return '個人';
				                             } else {
				                                 return '群祖';
				                             }
				                         }
				                     },

            ],
			pager: "#gridPager",
			sortname: 'PRG_NO desc,USR_NO asc',
			viewrecords: true,
			loadComplete: function () {
			    $(this).find(">tbody>tr.jqgrow").addClass("myAltRowClassEven2");
			    //$(this).find(">tbody>tr.jqgrow:odd").addClass("myAltRowClassEven");
			    //$(this).find(">tbody>tr.jqgrow:even").addClass("myAltRowClassEven");
			}
        });
        $("#btn_search").click(function () {
            $gridList.jqGrid('setGridParam', {
                postData: { keyword: $("#txt_keyword").val() },
            }).trigger('reloadGrid');
        });
    }
    function btn_add() {
        $.modalOpen({
            id: "Form",
            title: "新增資料",
            url: "/ERPManage/SS03/Form",
            width: "800px",
            height: "400px",
            callBack: function (iframeId) {
                top.frames[iframeId].submitForm();
            }
        });
    }
    function btn_edit() {
        var keyValue = $("#gridList").jqGridRowValue().SID;
        $.modalOpen({
            id: "Form",
            title: "修改資料",
            url: "/ERPManage/SS03/Form?keyValue=" + keyValue,
            width: "800px",
            height: "400px",
            callBack: function (iframeId) {
                top.frames[iframeId].submitForm();
            }
        });
    }
    function btn_delete() {
        $.deleteForm({
            url: "/ERPManage/SS03/DeleteForm",
            param: { keyValue: $("#gridList").jqGridRowValue().SID },
            success: function () {
                $.currentWindow().$("#gridList").trigger("reloadGrid");
            }
        })
    }
    function btn_details() {
        var keyValue = $("#gridList").jqGridRowValue().SID;
        $.modalOpen({
            id: "Details",
            title: "查看資料",
            url: "/ERPManage/SS03/Details?keyValue=" + keyValue,
            width: "680px",
            height: "500px",
            btn: null,
        });
    }
</script>

<div class="topPanel">
    <div class="toolbar">
        <div class="btn-group">
            <a class="btn btn-info" onclick="$.reload()"><span class="glyphicon glyphicon-refresh"></span></a>
        </div>
        <div class="btn-group">
            <a id="NF-add" authorize="yes" class="btn btn-info dropdown-text" onclick="btn_add()"><i class="fa fa-plus"></i>新建資料</a>
        </div>
        <div class="operate">
            <ul class="nav nav-pills">
                <li class="first">已選中<span>1</span>項</li>
                @*<li><a id="NF-edit" authorize="yes" onclick="btn_edit()"><i class="fa fa-pencil-square-o"></i>修改資料</a></li>
                <li><a id="NF-delete" authorize="yes" onclick="btn_delete()"><i class="fa fa-trash-o"></i>刪除資料</a></li>*@
                <li><a id="NF-Details" authorize="yes" onclick="btn_details()"><i class="fa fa-search-plus"></i>查看資料</a></li>
            </ul>
            <a href="javascript:;" class="close"></a>
        </div>
        <script>$('.toolbar').authorizeButton()</script>
    </div>
    <div class="search">
        <table>
            <tr>
                <td>
                    <div class="input-group">
                        <input id="txt_keyword" type="text" class="form-control" placeholder="資料名稱/資料編號" style="width: 200px;">
                        <span class="input-group-btn">
                            <button id="btn_search" type="button" class="btn  btn-info"><i class="fa fa-search"></i></button>
                        </span>
                    </div>
                </td>
            </tr>
        </table>
    </div>
</div>
<div class="gridPanel">
    <table id="gridList"></table>
    <div id="gridPager"></div>
</div>

